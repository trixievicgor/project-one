{
  "name": "marketvision",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && npm install --prefix frontend",
    "dev": "npx wrangler pages dev frontend/dist --compatibility-flags=nodejs_compat -- live --port 5173",
    "build": "npm run build --prefix frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260301.1",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "tree-kill": "^1.2.2",
    "yahoo-finance2": "^3.13.1"
  }
}
